import { StyleSheet } from 'react-native';

export const tabIconStyles = StyleSheet.create({
	layout: {
		backgroundColor: 'red',
	},
	baseText: {
		fontFamily: 'Arial',
		fontSize: 18,
		fontWeight: 'bold',
	},
});